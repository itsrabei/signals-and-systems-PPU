\begin{tikzpicture}
	\begin{axis}[
		width=12cm,
		height=7cm,
		title={Impulse Response $h(t) = A[u(t) - u(t-T)]$},
		xlabel={$t$},
		ylabel={$h(t)$},
		axis lines=middle,
		xmin=-1, xmax=3.5,
		ymin=0, ymax=1.2,
		xtick={2},
		xticklabels={$T$},
		ytick={1},
		yticklabels={$A$},
		grid=major,
		grid style={line width=.1pt, draw=gray!30},
		]
		
		% Draw the rectangular pulse shape
		\draw[red, very thick]
		(axis cs:-1, 0) -- (axis cs:0, 0)     % Zero line
		-- (axis cs:0, 1)                     % Rising edge
		-- (axis cs:2, 1)                     % Top edge
		-- (axis cs:2, 0)                     % Falling edge
		-- (axis cs:3.5, 0);                  % Zero line
		
	\end{axis}
\end{tikzpicture}